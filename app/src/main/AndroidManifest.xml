<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.developerskatta.androiddesignapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentDetailsActivity" />
        <activity android:name=".GridRecyclerViewActivity"></activity>
        <activity android:name=".StudentModelRecyclerViewActivity" />
        <activity android:name=".StudentListActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".LoginScreenWithRelativeLayoutActivity" />
        <activity android:name=".RelativeLayoutActivity" />
        <activity android:name=".LinearLayoutGravityActivity" />
        <activity android:name=".LoginScreenActivity">

        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>